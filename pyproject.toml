[project]
name = "whistx"
version = "0.1.0"
description = "whistx application settings for uv"
requires-python = ">=3.12"

[tool.uv]
allow-insecure-host = [
  "localhost",
  "127.0.0.1",
  "host.containers.internal",
]
